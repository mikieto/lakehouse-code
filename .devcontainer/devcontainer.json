{
  "name": "Lakehouse Code",
  "image": "ghcr.io/mikieto/lakehouse-code:latest",
 // "initializeCommand": "chown -R dev:dev ${WORKSPACE_FOLDER} || true",

  "initializeCommand": "WS=$(pwd); chown -R 1000:1000 \"$WS\" \"$WS/.git\" || true",
 
  "features": { "ghcr.io/devcontainers/features/docker-in-docker:2": {} },
  "remoteUser": "dev",
  "postCreateCommand": "cd \"$PWD\" && pre-commit install -t pre-commit -t commit-msg"
}
